## Personal Site

This is a Next.js React SPA deployed on Cloudflare Workers bootstrapped with ```npm create cloudflare@latest -- my-next-app --framework=next```.

## Deploy

### Local NodeJS runtime
```npm run deploy```

### Local Cloudflare ```workerd``` runtime
```npm run preview```

### Production Cloudflare
```npm run deploy```
